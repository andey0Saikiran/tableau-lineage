node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
*.tsbuildinfo
npm-debug.log*
yarn-error.log*

# Workflow files need the gh "workflow" OAuth scope to push; add via GitHub web UI
.github/workflows/

# Agent/scratch test files must never be swept in by `git add -A`
test/_*.test.ts
test/**/_*.test.ts

# MCPB build output (the .mcpb is attached to a GitHub Release, not committed)
mcp/build-mcpb/
mcp/*.mcpb
